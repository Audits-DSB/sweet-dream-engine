import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function InventoryPage() {
  return <PlaceholderPage title="Client Inventory" description="Lot-based inventory tracking per client" />;
}
